# Web
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2

# The AI team — Nebius token factory, OpenAI-compatible (specs/v4.2)
openai==2.7.1
# Checker (anti-hallucination) — HHEM-2.1-Open is a classifier, not a chat
# model; run directly via transformers, not through the OpenAI-compatible
# chat endpoint (specs/v4.2/01#the-checker-is-a-direct-classifier-call-not-a-chat-completion)
transformers==4.57.1
# CPU-only build, from PyTorch's own index below — the default PyPI wheel
# bundles CUDA and is several GB; this classifier never needs a GPU, and
# the full wheel exhausted disk space on the production deploy (a real
# incident, not a hypothetical: it failed the first deploy of specs/v6).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0

# Core store + vaults — Postgres, replacing sqlite3 (specs/v6)
psycopg2-binary==2.9.11

# Knowledge library
neo4j==6.2.0

# Source ingestion
pypdf==6.14.2

# v2 — accounts, sessions
bcrypt==5.0.0
itsdangerous==2.2.0

# v2 — Pro plan billing
stripe==15.5.0
